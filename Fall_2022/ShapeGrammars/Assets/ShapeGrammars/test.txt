#define maxDepth 9
#define maxOper 100
#var x 3

#var y 1

%%

tree: 
	(.1){ T(1, 1, 1) AtDepth(3, sgrule{ box() }) tree() }
	(.1){ T(1, 2, 2) AtDepth(3, sgrule{ box() }) tree() }

box: { PlaceShape("Box") }